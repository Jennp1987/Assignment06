/*eslint-env browser*/

//STEP 1

/*
function abValue() {
    "use strict";
    var userNum = window.prompt("Enter a positive or negative integer"),
        absoValue = Math.abs(userNum);
    return absoValue;
}
window.console.log(abValue());
*/


//STEP 2

/*
function inputRound() {
    "use strict";
    var userNum = window.prompt("Enter a floating point value, a.k.a. a decimal"),
        round = Math.round(userNum);
    return round; 
}

window.console.log(inputRound());

*/
//STEP 3

/*
function inputRoundDown() {
    "use strict";
    var userNum = window.prompt("Enter a floating point value, a.k.a. a decimal"),
        roundDown = Math.floor(userNum);
    return roundDown;
}

window.console.log(inputRoundDown());
*/


//STEP 4


/*
function highLow() {
    "use strict";
    var userNum = window.prompt("Enter five numbers separated by commas").split(','),
        output = "The lowest number is: " + Math.min( ...userNum ) + ". The highest number is: " + Math.max( ...userNum );
    return output; 
}
window.console.log(highLow());
*/


//STEP 5

/*
function square() {
    "use strict";
    var userNum = window.prompt("Enter in a number and I'll give its square root"),
        squareIt = Math.sqrt(userNum);
    return squareIt;
}

window.console.log(square());
*/







